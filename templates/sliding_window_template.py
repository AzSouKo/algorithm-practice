"""
LeetCode #XXX - 题目名称
难度：简单 / 中等 / 困难
标签：滑动窗口 / 数组 / 哈希表
日期：YYYY-MM-DD
思路：简要说明解题思路（定长/不定长？用什么数据结构维护窗口？）
时间复杂度：O(n)
空间复杂度：O(k)  # k 为窗口大小或字符集大小
"""


def solution(nums):
    """
    函数功能说明
    :param nums: 输入参数说明
    :return: 返回值说明
    """
    # TODO: 写你的代码
    pass


if __name__ == "__main__":
    # 测试用例
    test_cases = [
        # (输入, 期望输出),
    ]
    for i, (inp, expected) in enumerate(test_cases):
        result = solution(inp)
        status = "✅" if result == expected else f"❌ (got: {result})"
        print(f"测试 {i + 1}: {status}")
